services:
  chandra-api:
    build: .
    ports:
      - "8080:8080"
    env_file:
      - local.env
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
